services:
  prod:
    build:
      context: .
      dockerfile: Dockerfile
      target: final
    container_name: imageto3d_prod
    image: imageto3d-prod
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
    devices:
      - /dev/dri:/dev/dri